import { StyleSheet } from 'react-native';


const styles = StyleSheet.create({
  loaderAndEmptyStateView: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center'
  },
  main: {
    flex: 1
  }
});

export default styles;