const { default: strings } = require("./strings");

export const noImagePickerData = [strings.camera, strings.photoLibrary, strings.cancel];

export const hasImageAlreadyPickerData = [strings.camera, strings.photoLibrary, strings.remove, strings.cancel];

